# Required at runtime.
DATABASE_URL=postgres://user:pass@host/db

# Shared secret for the draft-order ESPN sync endpoint.
# GET /api/sync-draft-order?secret=<value>  — or send x-sync-secret header.
# Leave unset to disable the endpoint (it returns 500).
SYNC_SECRET=change-me-in-local-and-prod
